This module adds validation for orders in payment state when closing a POS session.

Features:
- Prevents closing session if there are orders with pending payments
- Shows warning with the number of pending orders
- Option to force close the session
